"use strict";
{
    const getPropertyValue = (obj, key) => {
        return obj[key];
    };
    const user = {
        name: 'Monir',
        age: 23,
        address: 'jsr'
    };
    const result1 = getPropertyValue(user, 'emni');
    //
}
